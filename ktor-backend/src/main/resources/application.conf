ktor {
  deployment {
    port = 8080
    host = "0.0.0.0"
    watch = [ com.traffic ]
  }

  server {
    development = true
  }

  application {
    modules = [ com.traffic.ApplicationKt.module ]
  }

  ai {
    endpoint = "http://localhost:8000/api/process-video"
  }
}
